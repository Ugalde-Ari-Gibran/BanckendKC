spring:
  application:
    name: BanckendKC
  datasource:
    url: jdbc:postgresql://localhost:5433/kamycarnes
    username: augalde
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

jwt:
  secret: KamyCarnes2024SecretKeyMuyLargaParaQueSeaSegura256Bits!!XD
  expiration-ms: 86400000  # 24 horas
