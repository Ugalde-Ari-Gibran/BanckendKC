spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/KamyCarnesDev
    username: augalde
    password: DevPasswordSegura
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:devsecret}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
