spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/KamyCarnesProd
    username: augalde
    password: ProdPasswordUltraSegura
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:prodlocalsecret}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
